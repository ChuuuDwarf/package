我要把一支獨立的甘特圖 script 併進現有的網站。

【現況】
- 網站：<框架，例如 FastAPI + Next.js>
- 現在圖是怎麼來的：<例如「後端用 Plotly 產 JSON，前端 render」>
- 相關檔案我附了：<路由檔、現有圖表元件>

【要併的東西】
gantt.py（附檔）：讀 CSV 資料夾，算每個 step 的 runtime，
產出每檔一張甘特圖 + 一張跨檔平均圖。

【我要的接縫】
<明確講：要 JSON API？要 PNG endpoint？還是要能被 import 的 library？>

【限制】
- 不要改動現有網站既有的行為
- 先讀我附的檔案，講你打算怎麼拆，等我同意再動手

【已知問題，一起處理】
- 空 CSV（只有表頭）會讓整批 crash
- 時間戳有 CST，白名單外的格式會靜默退回 cumsum
